"""simulator.py – Simulates credit-limit offers and customer responses.

Generates synthetic customer data, applies credit-limit actions chosen by
a bandit policy, and returns observed rewards (e.g. utilisation, default
probability, revenue) for each interaction.
"""
