"""reward.py – Reward function definitions for the credit-limit bandit.

Encapsulates the business logic that converts raw simulation outcomes
(utilisation, default, revenue) into scalar reward signals consumed by
bandit algorithms.
"""
